-- إنشاء قاعدة البيانات
CREATE DATABASE IF NOT EXISTS ci4_blog CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- استخدام قاعدة البيانات
USE ci4_blog;

-- إظهار رسالة نجاح
SELECT 'تم إنشاء قاعدة البيانات ci4_blog بنجاح!' as message;
